{
  "name": "twitter-test",
  "version": "1.0.0",
  "scripts": {
    "build": "babel src -d lib",
    "start": "yarn build && node lib/index.js"
  },
  "license": "MIT",
  "dependencies": {
    "twitter": "^1.7.1"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.3",
    "babel-preset-env": "^1.7.0"
  }
}
